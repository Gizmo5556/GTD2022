if pierced == other.id {
    if exploded <= 0 exit else { if other.upgraded exploded -= 0.5 else exploded -- }
}

audio_play_sound(sndBossHit,0,0)

if hp <= 0 {

thing = other.id;

event_user(0)

instance_destroy()
global.money += scrGetWorth(object_index);

}
else { if other.upgraded hp -= 10 else hp -- }

